#include <bits/stdc++.h>
using namespace std;

int main()
{
    std::ios::sync_with_stdio(0);
    string s;
    cin >> s;
    char d[s.length()];
    for(int i=0;i<s.length();i++)
        d[i] = s.at(i);

}
