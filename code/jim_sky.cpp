#include <bits/stdc++.h>

#define lli long long int
#define ull unsigned long long int

using namespace std;
const int MX = 100000;
int n;
int a[MX];
inline void solve()
{
    cin >> n;
    for(int i=0;i<n;i++)
        cin >> a[i];
    
}

int main()
{
    std::ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t = 1;
    //cin >> t;
    while(t-- > 0)
        solve();
    return 0;
}
